# 实验升级与回退

- 局部实验有效且未触边界：继续局部验证，不必扩大空间。
- 局部窗口触边界：只有绝对 Validator 边界仍有空间时才允许按已注册策略扩大。
- 触及绝对边界：停止外扩，把它作为模型/结构诊断证据。
- Gate 退化：ROLLBACK，并降低当前假设优先级。
- 多轮无新增证据：由 Campaign plateau/restart policy 决定是否停止或重启，而不是 Skill 自己宣布收敛。