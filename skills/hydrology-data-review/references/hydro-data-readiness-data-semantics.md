# 数据语义检查

Hydro-Agent 的数据检查不是只看“有没有值”，而是确认数据是否能被当前模型语义正确解释。

- 降水：确认累计/时段定义与时间步长。
- 蒸散发字段：确认是潜在蒸散发、实测蒸发还是其他语义；禁止仅凭字段名猜测。
- 流量：确认断面、单位、时区和时间聚合方式。
- 流域属性：只把来源明确、单位可追溯的属性作为先验。
- 初始状态：必须与起算时刻和 warmup 协议一致。

任何语义不确定性都应进入 Evidence，而不是被默认值静默掩盖。